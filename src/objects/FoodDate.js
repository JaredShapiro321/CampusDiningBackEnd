class FoodDate {
    constructor(title) {
        this.title = title;

        let titleArray = str.split(", ");

        this.date = date;
        this.time = time;
        this.day = split[0];
    }
}